import { styled } from '@mui/material/styles';

export const FooterContainer = styled('div')({
    width: '100%',
    height: 150,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 10,
    color: '#fff',
});
